# Claude API
ANTHROPIC_API_KEY=

# LINE
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=

# System Admins (comma-separated LINE userIds)
# Falls back to ADMIN_USER_IDS if not set
SYSTEM_ADMIN_IDS=Uadmin1,Uadmin2

# Users
USER_STORE_PATH=data/users.json                       # optional
WORKSPACE_STORE_PATH=data/workspaces.json              # optional
PENDING_ACTION_STORE_PATH=data/pending-actions.json    # optional
WORKSPACE_DATA_DIR=data/workspaces                     # optional

# Agent
AGENT_MODEL=                                           # optional, default: claude-haiku-4-5-20251001
AGENT_MAX_TOKENS=                                      # optional, 미설정 시 Models API에서 자동 조회 (폴백: 4096)
AGENT_MAX_TURNS=                                       # optional, default: 15
AGENT_MAX_TOKEN_RETRIES=                               # optional, default: 3 (max_tokens 도달 시 자동 resume 횟수)
AGENT_COMPACT_MODEL=                                   # optional, default: claude-sonnet-4-6
AGENT_COMPACT_MAX_TOKENS=                              # optional, 미설정 시 Models API에서 자동 조회 (폴백: 4096)

# MCP Pool
MCP_POOL_SIZE=3                                        # optional, default: 3
# PUPPETEER_EXECUTABLE_PATH=                            # Docker: 자동 설정. 로컬: 미설정 시 Puppeteer 기본값

# Logging
LOG_LEVEL=                                             # optional: debug/info/warning/error, default: info
DEBUG=                                                 # optional: 1 or true → debug level (LOG_LEVEL takes priority)

# Server
PORT=3000

# Google OAuth (GWS API 전환)
GOOGLE_CLIENT_ID=                                     # Google Cloud Console OAuth Client ID
GOOGLE_CLIENT_SECRET=                                 # Google Cloud Console OAuth Client Secret
GOOGLE_REDIRECT_URI=https://your-tunnel.example.com/auth/google/callback
TOKEN_ENCRYPTION_KEY=                                 # AES-256-GCM 마스터 키 (32바이트 = 64 hex 문자)
# 키 생성: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Cloudflare Tunnel은 이 레포 내 `deploy/_shared/cloudflared/`의 공용 단일 tunnel
# 컨테이너가 담당. 앱 서비스는 CF_TUNNEL_TOKEN이 필요 없으며 cf_tunnel 외부 Docker
# 네트워크를 통해 tunnel 컨테이너가 접근한다.
